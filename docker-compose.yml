services:
  viofo-backup:
    build: .
    user: "1000:1000"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - dashcam-data:/data
      - dashcam-logs:/logs
    restart: unless-stopped

volumes:
  dashcam-data:
  dashcam-logs:
